server:
  port: 9090

spring:
  application:
    name: Razorpay

  datasource:
    url: jdbc:mysql://localhost:3306/razorpayDB
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

payment:
  order:
    default-order-expiry-minutes: 30
